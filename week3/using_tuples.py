my_tuple = (1,2,3,4,5)
# print(my_tuple[0 + my_tuple[1]])
# print(my_tuple[-1]+ my_tuple[-2])

#we can change the values of list items by accessing the item via idex, then assigning to it a new value
# my_tuple[3] = 1...#we can't do this with tuples, tuples are immutable

my_tuple = (5,4,3,2,1)
print(my_tuple)


